# ============================================================================
# KADABRA ENVIRONMENT VARIABLES
# ============================================================================
# Copy this to .env.local for local development
# IMPORTANT: Also add these to Vercel Dashboard -> Settings -> Environment Variables
# ============================================================================

# ----------------------------------------------------------------------------
# Authentication (Required)
# ----------------------------------------------------------------------------
NEXTAUTH_URL="http://localhost:3001"
NEXTAUTH_SECRET="your-secret-here"

# ----------------------------------------------------------------------------
# Cross-app URLs for SSO
# ----------------------------------------------------------------------------
NEXT_PUBLIC_ABRA_URL="http://localhost:3000"
NEXT_PUBLIC_KADABRA_URL="http://localhost:3001"

# ----------------------------------------------------------------------------
# Video Clipper (Required for /admin/tools/video-clipper)
# ----------------------------------------------------------------------------
# Replicate API - for AI video processing (transcription, trimming, captions)
# Get from: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN="r8_xxx"

# Vercel Blob - for storing uploaded videos and generated clips
# Get from: Vercel Dashboard -> Storage -> Blob -> Tokens
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxx"

# Google Gemini API - for AI analysis of marketing moments
# Get from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY="AIzaSyxxx"

# ----------------------------------------------------------------------------
# Pusher (Required for realtime notifications)
# ----------------------------------------------------------------------------
PUSHER_APP_ID="your-app-id"
PUSHER_SECRET="your-secret"
NEXT_PUBLIC_PUSHER_KEY="your-key"
NEXT_PUBLIC_PUSHER_CLUSTER="us3"

# ----------------------------------------------------------------------------
# Database (Usually set automatically by Vercel)
# ----------------------------------------------------------------------------
# DATABASE_URL="postgresql://..."

# ============================================================================
# DEPLOYMENT CHECKLIST
# ============================================================================
# When adding new env vars:
# 1. Add to this .env.example file with description
# 2. Add to your local .env.local
# 3. Add to Vercel: Dashboard -> [project] -> Settings -> Environment Variables
# 4. Redeploy after adding to Vercel
# ============================================================================
