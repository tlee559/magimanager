// Re-export from shared auth package
export {
  Permissions,
  getAccessibleViews,
  abraPermissions,
  kadabraPermissions,
  canAccessAbra,
  canAccessKadabra,
} from '@magimanager/auth';
export type { UserRole } from '@magimanager/auth';
